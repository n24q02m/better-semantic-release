"""
better-semantic-release additions (bsr): built-in release-safety guards.

This subpackage is NET-NEW to the fork. Upstream never touches it, so
`git merge upstream/master` never conflicts here.
"""
